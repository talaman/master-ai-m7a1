```
pip install -r requirements.txt
cd app
python index.py
```
devcontainer
requirements.txt
.env

```

rasa init
rasa train
rasa shell
rasa run actions
rasa run -m models --enable-api --cors "*" --debug
```